apply plugin: 'com.android.application'

dependencies {
    compile project(':Library')
}


android {
    compileSdkVersion 22
    buildToolsVersion '22'

    defaultConfig {
    	applicationId "fr.castorflex.android.flipimageview.sample"
        minSdkVersion 15
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }
}
